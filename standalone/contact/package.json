{
  "name": "hitobito-contact",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check worker/index.mjs && node --test scripts/contact.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "migrate:local": "wrangler d1 migrations apply hitobito-contact --local --config wrangler.jsonc",
    "migrate:remote": "wrangler d1 migrations apply hitobito-contact --remote --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
